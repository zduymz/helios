# 2026-04-24

## Did
- Finished Phase 1a: Qwen2.5-0.5B-Q4_K_M running in Docker via `ghcr.io/ggml-org/llama.cpp:server` on `localhost:8080`.
- Hit `/v1/models` and `/v1/chat/completions` — got coherent English back.
- Measured TTFT with `curl -w 'time_starttransfer'`: **~15 ms TTFT, ~605 ms total** for "count 1 to 20".

## Broke
- First TTFT attempt used `time curl ... --stream` — that's full completion time, not TTFT. Switched to `time_starttransfer` (first received byte) for the real number.

## Learned
- The OpenAI API is the lingua franca of inference serving. llama.cpp on CPU and vLLM on a GPU speak the same `/v1/*` surface — that's the whole premise of a model gateway (Phase 3).
- GGUF is llama.cpp's single-file packaging; Q4_K_M is the "go-to 4-bit medium quality" quant.
- On a laptop CPU with a 0.5B model, a short prompt streams first-byte in ~15 ms. Establishing this baseline before cloud GPU in Phase 1d — so I can speak to the *perf gap* vLLM actually buys.

## Next time
- Phase 1b: move this same container into kind as a `Deployment` + `Service`. Expect similar TTFT once the pod is Ready; the interesting number will be **time-from-apply to first response** (cold start without KServe). Compare vs 1c with KServe scale-to-zero.